{
  "name": "aboverepo",
  "displayName": "AboveRepo",
  "packageManager": "pnpm@10.8.0",
  "publisher": "mnismt",
  "license": "MIT",
  "description": "AboveRepo is a VSCode extension that allows you to view and interact with files in your repository.",
  "repository": {
    "type": "git",
    "url": "https://github.com/mnismt/aboverepo.git"
  },
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:aboveRepoFilesWebview"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "aboveRepo",
          "title": "Above Repo",
          "icon": "$(repo-clone)"
        }
      ]
    },
    "views": {
      "aboveRepo": [
        {
          "id": "aboveRepoFilesWebview",
          "name": "Files",
          "type": "webview",
          "contextualTitle": "Above Repo Files"
        }
      ]
    },
    "commands": []
  },
  "scripts": {
    "vscode:package": "pnpm run package && pnpm vsce package --no-dependencies",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "biome check src --apply",
    "test": "vscode-test",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "check-types": "tsc --noEmit",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "^20.14.9",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.14.1",
    "esbuild": "^0.23.0",
    "eslint": "^8.57.0",
    "glob": "^10.4.2",
    "mocha": "^10.5.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.2",
    "vsce": "^2.15.0",
    "@vscode/test-cli": "^0.0.13",
    "@vscode/test-electron": "^2.4.0"
  },
  "dependencies": {
    "@vscode-elements/elements": "^1.15.0",
    "@vscode/codicons": "^0.0.36",
    "fast-xml-parser": "^5.2.2",
    "ignore": "^7.0.4",
    "js-tiktoken": "^1.0.12"
  }
}